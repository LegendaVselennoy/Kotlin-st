spring:
  application:
    name: cource-kotlin

  datasource:
    username: postgres
    password: ${DB_PASSWORD:postgres}
    url: jdbc:postgresql://localhost:5432/test
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true

  profiles:
    active: test